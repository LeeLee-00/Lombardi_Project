1) place graphs in the input directory
2) run lombardi.py
3) resulting images are now in output directory

the relevant settings are at the bottom of the lombardi.py file (IO Settings) which allow to select the algorithm(s) to use
more advanced settings are at the top


